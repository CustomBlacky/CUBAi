const $=id=>document.getElementById(id);
function showTab(id,btn){document.querySelectorAll('.tab').forEach(x=>x.classList.add('hidden'));$(id).classList.remove('hidden');document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));btn.classList.add('active');$('title').textContent=id==='chat'?'CUBAi':'AI Render';$('subtitle').textContent=id==='chat'?'Ёвари зеҳни сунъии шумо':'Генератори тасвир бо AI'}
function quick(t){$('prompt').value=t;sendChat(new Event('submit'))}
function add(text,cls){const m=document.createElement('div');m.className='message '+cls;m.textContent=text;$('messages').appendChild(m);m.scrollIntoView({behavior:'smooth',block:'end'})}
async function sendChat(e){e.preventDefault();const box=$('prompt'),text=box.value.trim();if(!text)return;box.value='';add(text,'user');const loading=document.createElement('div');loading.className='message ai';loading.textContent='CUBAi фикр карда истодааст…';$('messages').appendChild(loading);try{const r=await fetch('/api/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:text})});const d=await r.json();loading.textContent=d.reply||d.error||'Хато шуд.'}catch(err){loading.textContent='Ба сервер пайваст шудан нашуд.'}}
async function generateImage(){const p=$('imagePrompt').value.trim();if(!p)return;const out=$('imageResult');out.innerHTML='<div class="loader">✨ Тасвир сохта шуда истодааст…</div>';try{const r=await fetch('/api/image',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt:p})});const d=await r.json();if(d.image)out.innerHTML='<img src="'+d.image+'" alt="CUBAi render">';else out.innerHTML='<div class="loader">'+(d.error||'Хато шуд.')+'</div>'}catch(e){out.innerHTML='<div class="loader">Хатои пайвастшавӣ.</div>'}}
function newChat(){$('messages').innerHTML='<div class="welcome"><div class="big-logo">C</div><h2>Чати нав 👋</h2><p>Саволатонро ба тоҷикӣ нависед.</p></div>'}
$('prompt').addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();sendChat(e)}})
